ISBN = input()

s = 0
target = (0,0)

for i in range(len(ISBN)-1):
    if ISBN[i] == "*":
        if i %2 !=0:
            target = (i, 3)
        else:
            target = (i, 1)
    else:
        if i % 2 != 0:
            s += int(ISBN[i]) * 3
        else:
            s += int(ISBN[i])*1

m = int(ISBN[-1])

for num in range(10):
    total = s + num * target[1] + m
    if total % 10 == 0:
        print(num)
        break
